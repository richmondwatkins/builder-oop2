.tree(data-id=tree._id.toString(), class=tree.getClass())
  .height #{(tree.height / 12).toFixed(2)} ft
  .controls
    if(tree.isGrowable)
      button.grow Grow
    if(tree.isChoppable)
      button.chop Chop
